server:
  port: 8081
  servlet:
    context-path: /Shopme
spring:
  datasource:
    password: '0000'
    url: jdbc:mysql://localhost:3306/shopmedb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 
            client-secret: 
            scope:
            - email
            - profile
    
    
    
    
    
    
    
    
    
    
    
    
    
    